{
  "name": "derek-sagers-portfolio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3000",
    "build": "vite build",
    "lint": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 3000"
  },
  "dependencies": {
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "vite": "^8.2.0"
  }
}
